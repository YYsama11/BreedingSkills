# Input Contract

## 必需文件

- `bam_list.tsv`
- 参考基因组 `genome.fa`

## `bam_list.tsv` 列要求

- 表头必须为：`sample_id	bam`
- `bam` 可以是绝对路径，也可以是相对当前工作目录的路径
- 所有 BAM 必须已经排序并建立索引

## 推荐最小元数据

- 每个样本只出现一次
- 不混用不同参考版本生成的 BAM
- 保留样本批次信息，便于后续排查批次效应

## 最终 VCF 建议

- 同时保留原始调用结果、标准化结果和过滤结果
- 所有过滤阈值都应记录在流程配置中
